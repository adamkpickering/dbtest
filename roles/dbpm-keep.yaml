kind: role
version: v7
metadata:
  name: dbpm-keep
spec:
  options:
    create_db_user_mode: keep
  allow:
    db_labels:
      "*": "*"
    db_names:
    - "*"
    db_permissions:
    - match:
        object_kind: table
        schema: public
      permissions:
        - SELECT
        - CREATE
